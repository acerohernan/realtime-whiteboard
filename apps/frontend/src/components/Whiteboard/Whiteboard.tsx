import { styled } from "@/theme";

const Wrapper = styled("div", {
  backgroundColor: "$gray400",
  width: "100%",
  height: "35vh",
});
const Whiteboard = () => {
  return <Wrapper />;
};

export default Whiteboard;
